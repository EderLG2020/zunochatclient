# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
zunochat-client.zip
.env
.env.local

# Assets generados por `npm run asset:generate` (ver ASSETS_OUTPUT_DIR en .env.example)
assets/generated/*
!assets/generated/.gitkeep

# HTML temporal que scripts/assets escribe al inyectar datos en un template
# (se borra solo al terminar; esto es solo una red de seguridad por si el
# proceso se corta a la mitad)
scripts/assets/templates/**/.render-*.html
